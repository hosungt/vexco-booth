# vexco-booth — 부스 세션 규칙

이 리포는 2026 과학·수학·정보 교사 컨퍼런스 부스의 1:1 바이브코딩 체험 전용이다.

1. 모든 출력은 한국어 존댓말. 선생님(방문 교사)이 화면을 함께 본다.
2. 부스 세션은 `/booth` 스킬 흐름을 우선한다. 관련 없는 긴 탐색·설명 금지, 간결·신속.
3. `templates/` 안의 엔진 코드(HTML/CSS/JS)는 수정 금지. 콘텐츠 생성 시
   `<script type="application/json" id="config">` 블록만 교체한다.
4. 산출물은 반드시 `workspace/<선생님이름>/` 아래에만 생성한다.
5. 세션 중 git commit/push 금지.
6. 학생 개인정보·실제 시험 문항 원문·유료 교재 내용을 입력받지 않는다.
